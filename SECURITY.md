# Security policy

Do not open a public issue containing API keys, private data, credentials, exact infrastructure secrets, or exploitable details. Revoke an exposed key immediately and remove it from Git history; deleting the visible line in a later commit is insufficient.

The example environment file contains placeholders only. The API has no authentication in local demonstration mode and must not be exposed publicly without an authenticated gateway, HTTPS, rate limiting, validation, and logging.

